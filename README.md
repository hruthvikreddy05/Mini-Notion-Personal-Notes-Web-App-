# Mini-Notion-Personal-Notes-Web-App-
Built a lightweight “mini Notion” web application using Python and SQLite to manage personal notes. The app supports full CRUD functionality-plus keyword search across note titles and contents. Notes are persisted in a local SQLite database and presented through a clean, responsive UI using Flask templates and custom CSS.
